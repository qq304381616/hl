<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hl.devices">

    <application
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".DeviceMainActivity"/>
        <activity android:name=".DeviceLayoutActivity"/>
        <activity android:name=".DeviceDrawableActivity"/>
        <activity android:name=".DeviceValuesActivity"/>
    </application>

</manifest>
