<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hl.systeminfo">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- 震动-->
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name="com.hl.systeminfo.contact.ContactsActivity" />
        <activity android:name=".SystemMainActivity" />
        <activity
            android:name=".LaunchModeActivityA"
            android:launchMode="singleTask" />
        <activity android:name=".LaunchModeActivityB" />
        <activity android:name=".AppsInfoListActivity" />
        <activity android:name=".FragmentActivity" />

        <service android:name=".MyService" />
    </application>

</manifest>
